package pe.edu.pucp.model.enums;

/**
 * Estados del ciclo de vida de una orden de produccion (RF012).
 */
public enum EstadoOrdenProduccion {
    PLANIFICADA,
    EN_PROCESO,
    CERRADA,
    ANULADA
}
